<div
    @toggle-graph-fullscreen.window="fullScreen = !fullScreen"
    data-graph-label-layout="{{ meta.graphLabelLayout }}"
    id="graph-component"
    x-bind:class="fullScreen ? 'graph graph-fs' : 'graph'"
    x-data="AtlasGraph.createComponentState()"
    x-effect="handleOverlayState($el)"
    x-init="await initialize($el)"
>
    <div class="graph-toolbar">
        <div id="graph-controls">
            <div class="depth-control">
                <label for="graph-depth">Depth</label>
                <div class="slider">
                    <input id="graph-depth" max="3" min="1" name="graph-depth" step="1" type="range" x-model.number="sliderDepth" x-on:change="commitDepth()" x-on:input="scheduleDepthCommit()"/>
                    <div class="slider-marks" aria-hidden="true">
                        <span>1</span><span>2</span><span>3</span>
                    </div>
                </div>
                <output id="depth-display" for="graph-depth" x-text="sliderDepth"></output>
            </div>
            <div class="ctrl-right" style="display: flex; gap: 8px;">
                <button id="global-graph-btn" type="button" aria-label="Open full graph" x-on:click="showFullGraph = true; fullScreen = true; $nextTick(() => { fullGraph = openFullGraph($el.closest('[data-graph-label-layout]')) })"><i aria-hidden="true" icon-name="globe"></i></button>
                <button id="graph-fs-btn" type="button" aria-label="Toggle graph fullscreen" x-on:click="fullScreen = !fullScreen"><i aria-hidden="true" icon-name="expand"></i></button>
            </div>
        </div>
    </div>
    <div id="link-graph" x-effect="if (graphData && depth) { graph = renderLocalGraph($el.closest('[data-graph-label-layout]')) }"></div>
    <div class="show" id="full-graph" style="display: none;" x-show="showFullGraph">
        <button id="full-graph-close" type="button" aria-label="Close full graph" x-on:click="closeFullGraph($el.closest('[data-graph-label-layout]')); showFullGraph = false; fullScreen = false;"><i icon-name="x" aria-hidden="true"></i></button>
        <div id="full-graph-container"></div>
    </div>
</div>
